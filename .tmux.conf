# Ctrl-a
set-option -g prefix C-a

# Vi keybindins in copy-mode
setw -g mode-keys vi

# Last window
bind-key C-a last-window

# Set status bar
set -g status-bg colour243
set -g status-fg white
set -g status-left '#[fg=green]#H'
set-window-option -g window-status-current-bg red
